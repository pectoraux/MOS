# ADR-0004 — Vercel-Class UX, AWS-Class Runtime

**Status:** Accepted / Frozen

The web experience may use a Vercel-like deployment model. The runtime/data substrate must support queues, durable workers, sandboxing, secrets, network controls and dedicated isolation; AWS is the preferred infrastructure class. The architecture is capability-frozen, not tied to individual AWS service names.
