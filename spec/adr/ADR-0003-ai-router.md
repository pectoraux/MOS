# ADR-0003 — MarketingOS Owns the AI Router

**Status:** Accepted / Frozen

MarketingOS owns task profiling, hard eligibility, selection, cascades, evaluation thresholds, cost/latency policy and escalation. OpenRouter may be consumed as a provider aggregation layer but is not the domain routing authority.
